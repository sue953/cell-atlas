# 细胞之间 · 全结构拆分与教学补全

面向高中生物课堂的动物细胞、成熟叶肉细胞互动页面。细胞质基质仅文字说明，不单独建模。

## 本地使用

本项目支持本地使用。用户已改为授权将当前网页版本上传到 `sue953/cell-atlas` 公开仓库，并通过 GitHub Pages 公开发布。仅上传网页所需文件、模型和必要说明，不上传聊天记录、API 凭据、临时文件或本地 Git 历史。未经明确要求，不上传到其他账户或平台。

Mac 双击「启动.command」，其他平台运行 `python3 launch.py`。需要 Python 3 与支持 WebGL 2 的浏览器。代码、三维引擎、模型、图片、解码器均随包附带，不依赖外部内容服务。不要直接双击 index.html。

## 这一版可以做什么

- 本地 UI 采用 popular-web-designs 的 Apple 设计参考：中性浅色观察区、深色半透明顶栏、统一蓝色交互和系统字体；不加载外部字体。拆分控制收进观察区底部，模型默认放大，名称随模型横向位置对齐。
- 已检查桌面和 390px 窄屏布局、12 类详情入口（7 个独立 GLB + 5 类教学补全）、两种视图切换、二维图、核糖体亚基操作及拆分/复原。此项为界面与交互检查，不代替生物学审核。
- 已接入用户在 Downloads/jpg 提供的 7 个 GLB：细胞核、线粒体、叶绿体、内质网、细胞膜、溶酶体、植物细胞壁。点击对应结构，默认打开「3D结构」，可切换「教学补全」；没有独立 GLB 的类别直接打开教学补全。
- 动物细胞 9 类、植物细胞 10 类，两者共 12 类结构；可点击模型或从列表选择。
- 双细胞对照中，拖动某个细胞所在区域只旋转该细胞，另一侧保持不动；滚轮或双指捏合统一缩放。特有细胞器标注跟随各自旋转，「复位」恢复两个细胞的初始角度。单细胞和详情视图保留原有旋转操作。
- 「植物特有／动物特有」在完整细胞上用蓝色高亮和名称引线标注，旋转时引线跟随对应结构。再次点击同一按钮关闭；开启标注自动复原细胞，开启拆分则关闭标注。原模型总览与教学示意均支持。
- 特有标注仅比较本图的动物细胞与成熟高等植物叶肉细胞：植物侧标叶绿体、中央液泡，动物侧标中心体。中心体也见于某些低等植物，不代表所有植物均无中心体；细胞壁不是细胞器，溶酶体不作为动物绝对独有的标记。
- 「只拆开细胞器／复原细胞」和滑杆控制拆开程度。拆开时仅显示可点击的细胞器部件，自动隐藏未分类背景、细胞膜和细胞壁，避免大块表面遮挡；边界结构仍可从右侧列表查看。复原后恢复背景原设置。
- 总览包含 31 个原模型表面部件：动物 15 个、植物 16 个；另有两组核糖体教学补建，共 33 个可选部件。
- 详情仅保留「3D结构」和「教学补全」，已移除原模型拆分视图及部件下拉框。网页不展示建模过程、来源说明和操作提示；技术与授权记录保留在文档中。
- 「教学补全」显示独立重建的剖切结构、结构标注与说明。核糖体可分开或结合大小亚基。
- 「下载模型」下载独立模型的网页副本，或导出当前教学模型。
- 「教学示意」打开布局经检查的完整教学总览，适合讲解细胞器与液泡的位置关系。
- 拖动旋转、滚轮缩放；Esc 关闭弹窗；键盘 Tab 可操作按钮。
- 「二维图」保留用户提供的二维造型参考，参考图本身不作为生物学准确性的证据。

## 必须说明的模型边界

### 用户提供的独立模型

7 个原 GLB 文件均未被修改。网页使用另存的压缩副本，未进行减面；每个模型原有约 150 万个三角面全部保留。Draco 对坐标、法线、UV 采用 16/12/14 位量化编码，贴图缩至 2048 后无损 WebP 编码，并把超出 glTF 合法范围的高光颜色系数限制到 0–1。它们不是字节级原文件，也不是重新建模。

原文件哈希、网页副本哈希、前后面数与文件大小见 `dist/assets/user-models/manifest.json`；解码、属性及格式检查见 `validation.json`。法线贴图使用渲染器生成的切线空间，格式校验对此给出兼容性提示，未检出格式错误。

用户模型的隐藏表面、纹理和膜结构不因接入页面就被视为科学认证。例如，核糖体应该位于粗面内质网的细胞质侧，素材若表现出腔内颗粒，不应按错误位置讲解。页面保留独立的教学补全视图与生物学讲解；技术限制集中保留在本文档。

### 原始细胞的拆分

“全结构拆分”指所有要求的结构类别都能分别选择与讲解，并不表示所有原表面均已自动变成完整、封闭、准确的细胞器。

原素材没有细胞器语义分组：
- 动物模型通过接缝位置连通性分析得到 9 个部分。其中 8 个独立部分对应 5 个线粒体、高尔基体、一个囊状结构和中心体；余下大型连通部分按位置、轮廓、部分颜色条件切分。
- 植物模型整体连通，所有原模型部件采用区域表面切分。切口未封闭，可能带少量邻近表面，不能用于宣称完整超微结构重建。
- 核糖体微小颗粒无法可靠辨认，分别另建大小亚基与分布。原有未确定小颗粒留在非选择背景中，不随意指定身份。
- 溶酶体与运输囊泡的功能身份是教学指定，不能只凭形状或颜色证明。
- 原素材液泡区域存在容易被误解为细胞器或骨架的图案。原表面视图保留它们并警示；教学补全中不将这些图案当作液泡内容物。

原始面互斥分配给某部件或未确定背景，不复制一个新部件覆盖旧部件。每个原始面恰好保留一次。非选择背景不是一个名叫“细胞质基质”的独立模型。

## 生物学细节

见「拆分与教学核对.md」。教学补全包括双层核膜与核孔、核仁与染色质，线粒体连续折叠内膜，叶绿体基粒与基质片层，粗面/光面内质网，叠置中空高尔基体膜囊，中心粒九组三联微管，核糖体两亚基，单层膜囊泡、溶酶体和液泡，以及细胞壁和磷脂双分子层的区别。

所有几何均为简化示意，不是电镜、摄影测量或分子模拟重建。颜色、膜厚、数量与比例经过调整。教学补全不是从原模型中自动恢复的隐藏细节。植物图特指成熟、含叶绿体的高等植物叶肉细胞，不代表全部植物细胞。具体术语和深度应与任课教师实际使用的教材版本一致。

## 来源与使用范围

两份原始 GLB 来自 [GordenSun / LearningCell](https://github.com/GordenSun/LearningCell)，固定版本 8666e6e66c7a6468f7b463099556902667582457。仓库注明“仅用于课堂教学与科普展示”；本包不将其解释为通用商业授权。原 GLB 文件未修改，不是本次混元 API 生成。

派生分区与来源记录在 `dist/assets/models/*-parts.json`、`*-parts.bin` 和 `SOURCE.md`。原模型部件保持原法线、UV、纹理；拆开动画仅移动部件，不改原始面。

引擎 Three.js 0.170.0 及加载/导出工具遵循 `dist/vendor/THREE-LICENSE.txt`；Draco 解码器遵循 `dist/vendor/draco/LICENSE`。

## 核对参考

- [上海科学技术出版社《普通高中教科书 生物学 必修第一册 分子与细胞》](https://www.sstp.com.cn/elementary1/798.html)：教材信息入口，不声称已对某一版每页逐条认证。
- [OpenStax Biology 2e · 真核细胞](https://openstax.org/books/biology-2e/pages/4-3-eukaryotic-cells)
- [OpenStax Biology 2e · 内膜系统](https://openstax.org/books/biology-2e/pages/4-4-the-endomembrane-system-and-proteins)

## 可复核记录

- `node scripts/verify-partitions.cjs`：验证原文件及分区哈希、面数守恒、每类结构覆盖、源 GLB 计数。
- `node scripts/audit-models.mjs`：检查所有教学几何的数值，以及教学总览是否有细胞器进入核内、液泡内或细胞边界外。
- `scripts/decode-cell.cjs`、`analyze-cell.cjs`、`project-cell.py`、`segment-cells.py` 保留分区步骤；Python 步骤需要 NumPy 与 Pillow。
- `scripts/prepare-user-models.cjs` 生成用户 GLB 的网页副本；`verify-user-models.cjs` 验证原文件未变、面数保留、解码属性有效和 glTF 格式。依赖安装在忽略目录 work/model-tools，不混入页面或交付包。
- 分区拓扑报告中的开放边是真实切口记录；不会把没有封口的分区报告成闭合实体。
